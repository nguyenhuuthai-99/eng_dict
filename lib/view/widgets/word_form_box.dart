import 'package:flutter/material.dart';

class WordFormBox extends StatelessWidget {
  const WordFormBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
