import 'package:flutter/material.dart';

class SigninScreen extends StatelessWidget {
  final String screenId = "SigninScreen";
  const SigninScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
